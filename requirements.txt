# Base install (CPU default). GPU users can instead install requirements-gpu.txt.
-r requirements-core.txt

# ONNX Runtime (CPU build)
onnxruntime>=1.17.0

# Optional extras for specific features live in requirements-optional.txt
